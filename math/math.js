const add = (x, y) => x + y;

const sub = (x, y) => x - y;

const mul = (x, y) => x * y;

const div = (x, y) => x / y;

const PI = 3.14159;

const math = {
    add: add,
    sub: sub,
    mul: mul,
    div: div,
    PI: PI
}

module.exports = math;